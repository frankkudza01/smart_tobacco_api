from django.apps import AppConfig


class WorldreadyConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.worldready"
    verbose_name = "Inclusion (i18n, UX, surveys)"
