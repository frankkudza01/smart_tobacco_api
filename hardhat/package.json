{
  "name": "tobacco-traceability-hardhat",
  "version": "1.0.0",
  "private": true,
  "description": "Local Ethereum node + deploy TobaccoTraceability.sol for Django/Web3 integration tests",
  "scripts": {
    "compile": "hardhat compile",
    "node": "hardhat node --hostname 0.0.0.0",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "test:solidity": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  }
}
